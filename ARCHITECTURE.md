# 🏗️ 프로젝트 아키텍처

## 시스템 구조 다이어그램

```
┌─────────────────────────────────────────────────────────────────┐
│                          사용자/관리자                             │
│                     (웹 브라우저)                                  │
└───────────────────────┬─────────────────────────────────────────┘
                        │
                        │ HTTPS
                        ↓
┌─────────────────────────────────────────────────────────────────┐
│                    React 프론트엔드                                │
│                   (Vercel/Netlify)                               │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐          │
│  │ Apply Page   │  │ Admin Page   │  │ Components   │          │
│  │ /apply       │  │ /admin       │  │ • ApplyForm  │          │
│  │              │  │              │  │ • AdminTable │          │
│  └──────────────┘  └──────────────┘  │ • PdfModal   │          │
│                                       └──────────────┘          │
│  ┌────────────────────────────────────────────────┐             │
│  │           API Utils (fetch)                    │             │
│  │  • submitApplication()                         │             │
│  │  • fetchSubmissionList()                       │             │
│  │  • fetchPdfUrl()                               │             │
│  └────────────────────────────────────────────────┘             │
└───────────────────────┬─────────────────────────────────────────┘
                        │
                        │ REST API (JSON)
                        │ POST / GET
                        ↓
┌─────────────────────────────────────────────────────────────────┐
│              Google Apps Script WebApp                          │
│                (Backend API)                                    │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │ doPost(e)                                                │  │
│  │  • JSON 파싱                                              │  │
│  │  • 데이터 검증                                             │  │
│  │  • PDF 생성 (createPdfFromTemplate)                       │  │
│  │  • Spreadsheet에 저장                                     │  │
│  │  • 응답 반환 (pdfViewUrl)                                 │  │
│  └──────────────────────────────────────────────────────────┘  │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │ doGet(e)                                                 │  │
│  │  • action=list: 전체 목록 반환                            │  │
│  │  • action=pdf: PDF URL 반환                              │  │
│  │  • 관리자 권한 체크 (선택)                                 │  │
│  └──────────────────────────────────────────────────────────┘  │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │ sendDailyBatch() [트리거: 매일 14:00]                     │  │
│  │  • Spreadsheet 조회                                      │  │
│  │  • send_due_date == 오늘 && sent_flag == false 필터      │  │
│  │  • MailApp.sendEmail() - PDF 첨부                        │  │
│  │  • sent_flag 업데이트                                    │  │
│  └──────────────────────────────────────────────────────────┘  │
└────┬──────────┬──────────┬──────────────────────────────────────┘
     │          │          │
     │          │          │
     ↓          ↓          ↓
┌─────────┐ ┌──────────┐ ┌──────────────┐
│Spreadsheet│ │Google   │ │Google        │
│(Database)│ │Docs     │ │Drive         │
│          │ │(Template)│ │(PDF Storage) │
│submissions│ │         │ │              │
└─────────┘ └──────────┘ └──────────────┘
     │                         │
     │                         │
     └─────────────────────────┘
                  │
                  │ MailApp.sendEmail()
                  ↓
          ┌───────────────┐
          │ Gmail (이메일)  │
          │ • 사용자에게    │
          │   PDF 발송     │
          └───────────────┘
```

## 데이터 플로우

### 1️⃣ 사용자 신청 플로우

```
[사용자] → [React 폼 입력]
    ↓
[폼 제출] → submitApplication(data)
    ↓
[POST /] → Apps Script doPost(e)
    ↓
┌──────────────────────────┐
│ 1. JSON 파싱             │
│ 2. 데이터 검증           │
│ 3. PDF 생성              │
│    • Docs 템플릿 복제     │
│    • 플레이스홀더 치환    │
│    • PDF 변환            │
│    • Drive 업로드        │
│    • 공유 링크 생성      │
│ 4. Spreadsheet 저장      │
│    • appendRow()         │
│    • send_due_date 설정  │
│    • sent_flag = false   │
│ 5. 응답 반환             │
└──────────────────────────┘
    ↓
[React] ← { ok: true, pdfViewUrl }
    ↓
[성공 메시지 표시]
    • "제출 감사합니다"
    • PDF 미리보기 (선택)
```

### 2️⃣ 관리자 대시보드 플로우

```
[관리자] → [/admin 접속]
    ↓
[useEffect] → fetchSubmissionList()
    ↓
[GET /?action=list] → Apps Script doGet(e)
    ↓
┌──────────────────────────┐
│ 1. action 파라미터 확인   │
│ 2. 관리자 권한 체크(선택) │
│ 3. Spreadsheet 조회      │
│    • getDataRange()      │
│    • 전체 행 읽기        │
│ 4. JSON 변환             │
│ 5. 응답 반환             │
└──────────────────────────┘
    ↓
[React] ← { ok: true, data: [...] }
    ↓
[AdminTable 렌더링]
    • 제출 목록 표시
    • 통계 계산
    • 발송 상태 표시
```

### 3️⃣ 자동 이메일 발송 플로우

```
[매일 14:00] → 시간 기반 트리거
    ↓
sendDailyBatch() 실행
    ↓
┌──────────────────────────────────┐
│ 1. Spreadsheet 전체 조회         │
│ 2. 필터링:                       │
│    • send_due_date == 오늘       │
│    • sent_flag == false          │
│    • pdfFileId 존재              │
│ 3. 각 행에 대해:                 │
│    • PDF 파일 가져오기 (Drive)   │
│    • MailApp.sendEmail()         │
│      - to: email                 │
│      - subject: "사업계획서..."  │
│      - attachments: [pdfFile]    │
│    • sent_flag = true 업데이트   │
│    • sent_at = 현재시각 업데이트 │
│ 4. 로그 기록                     │
└──────────────────────────────────┘
    ↓
[사용자 이메일 수신]
    • PDF 첨부파일
    • 안내 메시지
```

## 기술 스택 상세

### Frontend Stack

```
React 18.2
├── TypeScript 5.2
├── Vite 5.0 (빌드 도구)
│   ├── Fast HMR
│   ├── ES Modules
│   └── Optimized Build
├── React Router 6.21 (라우팅)
│   ├── /apply → Apply.tsx
│   ├── /admin → Admin.tsx
│   └── / → Redirect to /apply
├── Tailwind CSS 3.4 (스타일링)
│   ├── Utility-first
│   ├── Responsive Design
│   └── Custom Components
└── Fetch API (HTTP 통신)
    ├── submitApplication()
    ├── fetchSubmissionList()
    └── fetchPdfUrl()
```

### Backend Stack

```
Google Apps Script (V8 Runtime)
├── SpreadsheetApp
│   ├── openById()
│   ├── getDataRange()
│   └── appendRow()
├── DriveApp
│   ├── getFileById()
│   ├── createFile()
│   └── setSharing()
├── DocumentApp
│   ├── openById()
│   ├── makeCopy()
│   └── replaceText()
├── MailApp
│   └── sendEmail()
├── Utilities
│   └── formatDate()
└── Time-based Trigger
    └── Daily @ 14:00 (Asia/Seoul)
```

### Data Storage

```
Google Spreadsheet
├── Sheet: "submissions"
├── Columns (16개):
│   ├── A: timestamp (Date)
│   ├── B: name (String)
│   ├── C: email (String)
│   ├── D: phone (String)
│   ├── E: company_name (String)
│   ├── F: business_number (String)
│   ├── G: desired_support (String)
│   ├── H: business_idea (Text)
│   ├── I: target_market (Text)
│   ├── J: competitiveness (Text)
│   ├── K: pdf_file_id (String)
│   ├── L: pdf_view_url (URL)
│   ├── M: send_due_date (Date String)
│   ├── N: sent_flag (Boolean)
│   ├── O: sent_at (Date)
│   └── P: error (String)
└── Row 1: Headers (Bold, Gray Background)
```

## 컴포넌트 계층 구조

### React Component Tree

```
App.tsx
├── Router
    ├── Route "/" → Navigate to "/apply"
    ├── Route "/apply" → Apply.tsx
    │   ├── ApplyForm.tsx
    │   │   ├── 기업 정보 섹션
    │   │   │   ├── Input: companyName
    │   │   │   ├── Input: name
    │   │   │   ├── Input: businessNumber
    │   │   │   ├── Input: email
    │   │   │   ├── Input: phone
    │   │   │   └── Select: desiredSupport
    │   │   └── 사업 계획 섹션
    │   │       ├── Textarea: businessIdea
    │   │       ├── Textarea: targetMarket
    │   │       └── Textarea: competitiveness
    │   └── SuccessMessage.tsx (조건부)
    │       ├── 성공 아이콘
    │       ├── 안내 메시지
    │       └── PDF iframe (선택)
    └── Route "/admin" → Admin.tsx
        ├── Header
        │   ├── 제목
        │   └── 새로고침 버튼
        ├── Stats Cards (3개)
        │   ├── 전체 제출
        │   ├── 발송 완료
        │   └── 발송 대기
        ├── AdminTable.tsx
        │   ├── Table Header
        │   └── Table Body
        │       └── Row (각 제출 건)
        │           └── "보기" 버튼
        └── PdfModal.tsx (조건부)
            ├── Modal Header
            ├── PDF iframe
            └── 닫기 버튼
```

## API 인터페이스

### Request/Response 명세

#### POST / (사용자 신청)

**Request Headers**:
```
Content-Type: application/json
```

**Request Body**:
```typescript
{
  name: string                // 대표자 이름
  email: string               // 이메일
  phone: string               // 연락처
  companyName: string         // 회사명
  businessNumber: string      // 사업자등록번호
  desiredSupport: string      // 희망 지원과제
  businessIdea: string        // 사업 아이디어
  targetMarket: string        // 목표 시장
  competitiveness: string     // 경쟁력
}
```

**Response**:
```typescript
{
  ok: boolean
  data?: {
    pdfViewUrl: string        // Drive PDF 미리보기 URL
    message: string           // 성공 메시지
  }
  error?: string              // 에러 메시지
}
```

#### GET /?action=list (관리자 목록 조회)

**Query Parameters**:
```
action=list
```

**Response**:
```typescript
{
  ok: boolean
  data?: SubmissionRecord[]
  error?: string
}

interface SubmissionRecord {
  timestamp: string
  name: string
  email: string
  phone: string
  companyName: string
  businessNumber: string
  desiredSupport: string
  businessIdea: string
  targetMarket: string
  competitiveness: string
  pdfFileId: string
  pdfViewUrl: string
  sendDueDate: string
  sentFlag: boolean
  sentAt: string
  error: string
}
```

#### GET /?action=pdf&row=N (PDF URL 조회)

**Query Parameters**:
```
action=pdf
row=3                         // 행 번호 (0-based)
```

**Response**:
```typescript
{
  ok: boolean
  data?: {
    pdfViewUrl: string
  }
  error?: string
}
```

## 보안 및 권한

### Apps Script 권한 스코프

```json
{
  "oauthScopes": [
    "https://www.googleapis.com/auth/spreadsheets",      // Spreadsheet 읽기/쓰기
    "https://www.googleapis.com/auth/drive",             // Drive 파일 생성/읽기
    "https://www.googleapis.com/auth/documents",         // Docs 읽기/복제
    "https://www.googleapis.com/auth/script.send_mail",  // 이메일 발송
    "https://www.googleapis.com/auth/script.external_request"  // 외부 요청 처리
  ]
}
```

### 접근 제어

```
┌──────────────────┬───────────────────┬──────────────┐
│ 리소스            │ 권한              │ 대상         │
├──────────────────┼───────────────────┼──────────────┤
│ Spreadsheet      │ Owner             │ 본인         │
│ Template Docs    │ Reader            │ 본인         │
│ PDF Files        │ Anyone with link  │ 모든 사용자  │
│ Apps Script      │ Execute           │ 모든 사용자  │
│ Admin Dashboard  │ View (optional)   │ 관리자만     │
└──────────────────┴───────────────────┴──────────────┘
```

## 성능 최적화

### Frontend 최적화

- **Code Splitting**: React Router 기반 lazy loading 가능
- **Tree Shaking**: Vite가 자동으로 사용하지 않는 코드 제거
- **CSS Purging**: Tailwind CSS가 사용하지 않는 스타일 제거
- **Minification**: Vite가 프로덕션 빌드 시 자동 압축

### Backend 최적화

- **Batch Processing**: `sendDailyBatch()`가 한 번에 여러 이메일 처리
- **Caching**: Drive 파일 ID를 Spreadsheet에 저장하여 재생성 방지
- **Lazy Loading**: PDF는 요청 시에만 생성 (템플릿 복제)

### 할당량 관리

```
Google Apps Script 무료 할당량:
├── 이메일 발송: 100통/일
├── 스크립트 실행: 90분/일
├── 실행 시간: 6분/실행
├── URL Fetch: 20,000회/일
└── Drive API: 일반적으로 충분
```

## 에러 처리

### Frontend 에러 처리

```typescript
try {
  const response = await submitApplication(data)
  if (response.ok) {
    // 성공 처리
  } else {
    // 에러 메시지 표시
    setError(response.error)
  }
} catch (error) {
  // 네트워크 에러
  setError('네트워크 오류')
}
```

### Backend 에러 처리

```javascript
try {
  // 메인 로직
} catch (error) {
  Logger.log('Error: ' + error)
  return createResponse(false, null, error.toString())
}
```

### 에러 로깅

- **Apps Script**: `Logger.log()` → Apps Script 실행 로그
- **Frontend**: `console.error()` → 브라우저 콘솔
- **Spreadsheet**: `error` 열에 에러 메시지 저장

## 확장 가능성

### 미래 개선 사항

1. **AI 통합**: OpenAI API로 사업계획서 자동 작성
2. **다국어 지원**: i18n 라이브러리 추가
3. **파일 업로드**: 사업자등록증, 로고 등 첨부
4. **결제 통합**: 프리미엄 컨설팅 서비스
5. **실시간 알림**: Firebase Cloud Messaging
6. **분석 대시보드**: Chart.js로 통계 시각화
7. **사용자 계정**: 로그인 기능 추가
8. **버전 관리**: PDF 수정 히스토리

---

**작성일**: 2025년 1월
**버전**: 1.0.0
